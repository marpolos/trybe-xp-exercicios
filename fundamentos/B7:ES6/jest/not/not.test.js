/* const days = require("./not.js");

describe("Day of week", () => {
    test('Sunday is a week day', () => {
        expect(days.weekDays).toContain('Sunday');
      });
      
      test('Sunday is not a workday', () => {
        expect(days.workDays).not.toContain('Sunday');
      });
})
 */