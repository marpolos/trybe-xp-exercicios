const printName = function () {
    const myName = "Lucas";
    return myName;
  }
  console.log(printName());

  const second = () => 4+5;
  console.log(second());

  const third = tres => 3+7;
  console.log(third());

  const forth = (x, y) => {
      return x + y * 99;
   }
   console.log(forth(4,5));

   const fifth = () => (66+88);
   console.log(fifth());

   function arrow (x,y) {
       return x + y;
   }
   console.log(arrow(7,8));

   const flexa = function (x,y) {
       return x-y;
   }
   console.log(flexa(8,5));